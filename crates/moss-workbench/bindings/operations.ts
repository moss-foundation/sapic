// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import { Identifier } from "@repo/moss-common";
import { WorkspaceMode } from "@repo/moss-workspace";
import type { WorkspaceInfo } from "./types";

export type CreateWorkspaceInput = { name: string; mode: WorkspaceMode; openOnCreation: boolean };

export type CreateWorkspaceOutput = { id: Identifier };

export type DeleteWorkspaceInput = { id: Identifier };

export type DeleteWorkspaceOutput = { id: Identifier };

export type DescribeWorkbenchStateOutput = { prevWorkspaceId?: Identifier };

export type ListWorkspacesOutput = Array<WorkspaceInfo>;

export type OpenWorkspaceInput = {
  /**
   * We use the workspace name instead of its path because
   * all workspaces can only be stored within a single directory.
   */
  name: string;
};

export type OpenWorkspaceOutput = { id: Identifier };

export type UpdateWorkspaceInput = {
  id: Identifier;
  /**
   * A new name for the workspace, if provided,
   * the workspace will be renamed to this name.
   */
  name?: string;
};

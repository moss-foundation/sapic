{
  "name": "@repo/moss-settings",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --plugin=prettier-plugin-tailwindcss --write \"**/*.{ts,tsx,md}\"",
    "generate-schema": "npx vite-node index.ts"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*"
  },
  "dependencies": {
    "typescript": "^5.8.2",
    "zod": "^3.25.30"
  }
}
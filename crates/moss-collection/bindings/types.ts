// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import { ChangeJsonValue, ChangeString, JsonValue } from "@repo/moss-bindingutils";
import type { EntryClass, EntryKind, EntryPath, EntryProtocol } from "./primitives";

/**
 * @category Type
 */
export type AddHeaderParams = {
  name: string;
  value: JsonValue;
  order: number;
  desc?: string;
  options: HeaderParamOptions;
};

/**
 * @category Type
 */
export type AddPathParamParams = {
  name: string;
  value: JsonValue;
  order: number;
  desc?: string;
  options: PathParamOptions;
};

/**
 * @category Type
 */
export type AddQueryParamParams = {
  name: string;
  value: JsonValue;
  order: number;
  desc?: string;
  options: QueryParamOptions;
};

/**
 * @category Type
 */
export type AfterUpdateDirEntryDescription = { id: string; path: EntryPath };

/**
 * @category Type
 */
export type AfterUpdateItemEntryDescription = { id: string; path: EntryPath };

/**
 * @category Type
 */
export type CreateDirEntryParams = { path: string; name: string; order: number; headers: Array<AddHeaderParams> };

/**
 * @category Type
 */
export type CreateItemEntryParams = {
  path: string;
  name: string;
  order: number;
  protocol?: EntryProtocol;
  queryParams: Array<AddQueryParamParams>;
  pathParams: Array<AddPathParamParams>;
  headers: Array<AddHeaderParams>;
};

/**
 * @category Type
 */
export type EntryInfo = {
  /**
   * Unique identifier for this entry
   */
  id: string;
  /**
   * Display name of the entry
   */
  name: string;
  /**
   * Path relative to the collection root.
   * Includes both the original path string and its segments.
   */
  path: EntryPath;
  /**
   * Classification of the entry (Request, Endpoint, Component, or Schema)
   */
  class: EntryClass;
  /**
   * Type of entry indicating its structure (Dir for directories, Item for files, Case of item cases)
   */
  kind: EntryKind;
  /**
   * HTTP protocol/method used by this entry, if applicable (GET, POST, PUT, DELETE, WebSocket, GraphQL, gRPC)
   */
  protocol?: EntryProtocol;
  /**
   * Determines the display position of this entry among others in the same group.
   * Entries are sorted in ascending order; lower values appear before higher ones.
   * Negative values are allowed and will be placed before positive values.
   * If multiple entries have the same order, they are sorted alphabetically.
   * If not specified, the entry appears last and is sorted alphabetically
   * among unspecified items.
   */
  order?: number;
  /**
   * Whether this entry is expanded in the tree view (applies to directories)
   */
  expanded: boolean;
};

/**
 * @category Type
 */
export type EnvironmentInfo = {
  id: string;
  name: string;
  /**
   * Determines the display position of this entry among others in the same group.
   * Entries are sorted in ascending order; lower values appear before higher ones.
   * Negative values are allowed and will be placed before positive values.
   * If multiple entries have the same order, they are sorted alphabetically.
   * If not specified, the entry appears last and is sorted alphabetically
   * among unspecified items.
   */
  order?: number;
};

/**
 * @category Type
 */
export type HeaderParamOptions = { disabled: boolean };

/**
 * @category Type
 */
export type PathParamOptions = { disabled: boolean };

/**
 * @category Type
 */
export type QueryParamOptions = { disabled: boolean };

/**
 * @category Type
 */
export type UpdateDirEntryParams = {
  id: string;
  /**
   * If provided, the directory will move to the new path
   * For example, if the new path is "requests/folder/", the name is "group"
   * The new relative path of the directory folder will be "requests/folder/group"
   */
  path?: string;
  name?: string;
  order?: number;
  expanded?: boolean;
};

/**
 * @category Type
 */
export type UpdateHeaderParamOptions = { disabled?: boolean };

/**
 * @category Type
 */
export type UpdateHeaderParams = {
  id: string;
  name?: string;
  value?: ChangeJsonValue;
  order?: number;
  desc?: ChangeString;
  options?: HeaderParamOptions;
};

/**
 * @category Type
 */
export type UpdateItemEntryParams = {
  id: string;
  /**
   * If provided, the entry will move to the new path
   * For example, if the new path is "requests/folder/", the name is "entry"
   * The new relative path of the entry folder will be "requests/folder/entry"
   */
  path?: string;
  name?: string;
  order?: number;
  expanded?: boolean;
  protocol?: EntryProtocol;
  queryParamsToAdd: Array<AddQueryParamParams>;
  queryParamsToUpdate: Array<UpdateQueryParamParams>;
  queryParamsToRemove: Array<string>;
  pathParamsToAdd: Array<AddPathParamParams>;
  pathParamsToUpdate: Array<UpdatePathParamParams>;
  pathParamsToRemove: Array<string>;
  headersToAdd: Array<AddHeaderParams>;
  headersToUpdate: Array<UpdateHeaderParams>;
  headersToRemove: Array<string>;
};

/**
 * @category Type
 */
export type UpdatePathParamOptions = { disabled?: boolean };

/**
 * @category Type
 */
export type UpdatePathParamParams = {
  id: string;
  name?: string;
  value?: ChangeJsonValue;
  order?: number;
  desc?: ChangeString;
  options?: PathParamOptions;
};

/**
 * @category Type
 */
export type UpdateQueryParamOptions = { disabled?: boolean };

/**
 * @category Type
 */
export type UpdateQueryParamParams = {
  id: string;
  name?: string;
  value?: ChangeJsonValue;
  order?: number;
  desc?: ChangeString;
  options?: QueryParamOptions;
};

[package]
name = "moss_bindingutils"
version = "0.1.0"
edition = "2024"

[dependencies]
ts-rs = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
const_export = { path = "const-export" }

[features]
integration-tests = []

[package]
name = "atomic_fs"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { workspace = true, features = ["full"] }
nanoid.workspace = true
anyhow.workspace = true

[features]
integration-tests = []
